import React from 'react';

export default function RegisterBar() {
    return (
        <div className="bar">
            <button variant="outlined" className="myButton" width="170px">Submit an Article</button>
        </div>
    )
}
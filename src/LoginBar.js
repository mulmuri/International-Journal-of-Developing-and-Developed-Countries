import React from 'react';

export default function LoginBar() {
    return (
        <div className="bar">
            <button variant="outlined" className="myButton">Login</button>
            <button variant="outlined" className="myButton">Register</button>
        </div>
    );
}